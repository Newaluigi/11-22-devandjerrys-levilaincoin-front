const ShowUser = (props) =>{
    return (
        <div>
               {/* {props.users
          ? props.users.map(props.users => {
            return (
            
                <p>{props.users.firstName}</p>
               
              
            )
          })
          : null} */}
   {/* <h1>{props.users.firstName[0]} </h1> */}


        {console.log(props)}
        </div>
    )
}
export default ShowUser